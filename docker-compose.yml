services:
  api:
    build: .
    ports: 
      - "8000:8000"
    develop:
      watch:
        - action: sync+restart
          path: .
          target: /app
        - action: rebuild
          path: requirements.txt
